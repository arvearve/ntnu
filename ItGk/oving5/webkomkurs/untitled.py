from random import randint, choice


def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	list = [choice(possible_doors) for n in range(0,size)]
	list[randint(0,size-1)] = '	T'
	return list


print build_table(10,4)

from random import randint, choice


def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	list = [choice(possible_doors) for n in range(0,size)]
	list[randint(0,size-1)] = '	T'
	return list


print build_table(10,4)from random import randint, choice


def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	return list




def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	list = [choice(possible_doors) f
	list[randint(0,size-1)] = '	T'
	return list




def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]

	list[randint(0,size-1)] = '	T'
	return list





def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	list = [choice(possible_doors) for n in range(0,size)]
	list[randint(0,size-1)] = '	T'
	return list


print build_table(10,4)from random import randint, choice


def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	list = [choice(possible_doors) for n in range(0,size)]
	list[randint(0,size-1)] = '	T'
	return list


print build_table(10,4)from random import randint, choice


def build_table(num_rows, size):
	table=[]
	for r in range(0,num_rows):
		table.append(build_row(size))
	return table

def build_row(size):
	possible_doors=['F','B', 0]
	list = [choice(possible_doors) for n in range(0,size)]
	list[randint(0,size-1)] = '	T'
	return list


print build_table(10,4)